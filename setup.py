from distutils.core import setup

setup(name='Pylicious',
      version='0.1',
      description='Python Delicious API Client. Supports both HTTP and OAUTH authentication.',
      author='Sean Brant',
      py_modules=['pylicious'])
